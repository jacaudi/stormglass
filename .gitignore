# --- managed by github-actions-align: regenerated on apply; add your own BELOW the end marker ---
# Base fragment; language fragments are appended. Rationale: github-actions-align/templates/README.md#ignore-files

# OS junk
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
Thumbs.db
desktop.ini

# Editor
*.swp
*.swo
*~
.idea/
*.iml
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json

# Merge/patch leftovers
*.orig
*.rej

# Secrets
.env
.env.*
!.env.example
*.pem
*.key
!**/testdata/**/*.pem
!**/testdata/**/*.key

# Local tooling
.worktrees/
bin/
dist/
coverage/
*.out
!.gitkeep

# Go
*.exe
*.exe~
*.dll
*.so
*.dylib
*.test
vendor/
go.work
go.work.sum

# kubebuilder / operator-sdk local state
*.kubeconfig
config/**/*.local.yaml

# Node / JS / TS — lockfiles ARE committed (npm ci depends on them)
node_modules/
.npm
.eslintcache
*.tsbuildinfo
.vite/
.next/
.nuxt/
.turbo/
npm-debug.log*
yarn-error.log*
# --- end managed ---
# Repo-specific entries below this line are preserved across regeneration.

# Project-specific
.secrets
chainguard-labels.*
CLAUDE.md
.private-journal/
deploy/.env

# Agent scaffolding
.superpowers/
docs/prompts/
# Implementation plans are local-only going forward. The plans already tracked
# under this path predate the rule and were deliberately left in place -- adding
# the ignore does not untrack them, so both regimes coexist by design.
docs/plans/
.auto-claude/

# UI screenshots (local scratch)
docs/screenshots/

# Built binaries (this repo's, by name — the managed block's bin/ does not cover
# a binary written to the repo root, which `go build .` does by default)
stormglass

# Go build cache and coverage formats the managed block does not carry
.cache/
*.coverprofile
coverage.txt
coverage.html

# Python — no Python project here today, retained from the pre-migration file
__pycache__/
*.pyc
.venv/
.pytest_cache/

# Extended macOS entries beyond the managed base
.AppleDouble
.LSOverride
Icon
.DocumentRevisions-V100
.fseventsd
.TemporaryItems
.VolumeIcon.icns
.com.apple.timemachine.donotpresent
.AppleDB
.AppleDesktop
Network Trash Folder
Temporary Items
.apdisk

# Linux
.fuse_hidden*
.directory
.Trash-*
.nfs*

# Eclipse-era IDE files
.project
.classpath
.settings/
